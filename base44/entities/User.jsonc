{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user",
        "speaker"
      ],
      "default": "user",
      "description": "User role: admin = full access, user = standard, speaker = speaker portal only"
    }
  }
}